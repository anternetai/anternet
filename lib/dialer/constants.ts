// Shared constants for the dialer / cockpit system

/** localStorage key used by the callback pipeline "Call Now" button to tell
 *  the cockpit which lead to jump to on load. */
export const DIALER_JUMP_TARGET_KEY = "dialer-jump-to-lead"
